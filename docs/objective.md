# Objective

Help a market-access writer pick the ZIN argument to reuse, from which
dossier and on which date, so they never have to scan the spreadsheet by hand.

If every metric is green and they still open the xlsx, the CLI failed.

The call is `./pakket cite`. Verdict first. Shortlist behind it.
